<UserControl x:Class="projektFeladat_WPF.UserControls.MessagesUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:projektFeladat_WPF.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900" MaxHeight="600">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Margin="0,40,0,0" Grid.Column="0"  AutoGenerateColumns="False" ItemsSource="{Binding SubjectList}" SelectedItem="{Binding SelectedItem}" Height="210" VerticalAlignment="Top" HorizontalAlignment="Left" Width="300">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding SubjectName}" Header="Sender:" />
                <DataGridTextColumn Binding="{Binding SubjectCode}" Header="Subject:" />
                <DataGridTextColumn Binding="{Binding Credits}" Header="Recieve date:" />
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid Grid.Column="0"  AutoGenerateColumns="False" ItemsSource="{Binding SubjectList}" SelectedItem="{Binding SelectedItem}" Height="210" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="300">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding SubjectName}" Header="Recipient:" />
                <DataGridTextColumn Binding="{Binding SubjectCode}" Header="Subject:" />
                <DataGridTextColumn Binding="{Binding Credits}" Header="Date sent" />
            </DataGrid.Columns>
        </DataGrid>
        <Label Content="Inbox:" HorizontalAlignment="Left" Margin="0,5,0,0" VerticalAlignment="Top" Height="35" Width="180" Grid.Row="0" FontSize="15"/>
        <Label Content="Sent messages" HorizontalAlignment="Left" Margin="0,0,0,210" Width="180" Grid.Row="1" FontSize="15" Height="35" VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
